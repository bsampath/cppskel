/* sqrt example */
#include <stdio.h>      /* printf */
#include <math.h>       /* sqrt */

#include "mysqrt.h"

double mysqrt(double param)
{
  double result;
  result = sqrt (param);
  return result;
}
